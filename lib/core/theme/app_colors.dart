import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary — warm orange
  static const primary = Color(0xFFFF6B35);
  static const primaryDark = Color(0xFFE55A2B);
  static const primaryLight = Color(0xFFFF8C5A);

  // Secondary — teal
  static const secondary = Color(0xFF00D4AA);
  static const secondaryDark = Color(0xFF00B894);

  // Accent & status
  static const accent = Color(0xFFFFD93D);
  static const danger = Color(0xFFEF4444);
  static const success = Color(0xFF00D4AA);

  // Backgrounds
  static const backgroundDeepest = Color(0xFF0A0A0C);
  static const backgroundBase = Color(0xFF0E0E10);
  static const backgroundCard = Color(0xFF16161A);
  static const backgroundCardHover = Color(0xFF1E1E24);
  static const backgroundInput = Color(0xFF1A1A1F);

  // Text
  static const textPrimary = Color(0xFFFAFAFA);
  static const textSecondary = Color(0xFFA1A1AA);
  static const textMuted = Color(0xFF71717A);

  // Borders — semi-transparent white
  static const border = Color(0x14FFFFFF); // 8% white
  static const borderLight = Color(0x1FFFFFFF); // 12% white
}
